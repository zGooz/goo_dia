/// textbox_refresh_karet()
// return: n/a

karx = xt + (collumn - left) * SYM_W; 
kary = yt + (row - top) * SYM_H;
karx1 = xt + (side_collumn - left) * SYM_W; 
kary1 = yt + (side_row - top) * SYM_H;             
